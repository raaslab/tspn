// Choose one of the two; if both are undefined then uses the 2-approximation
#define USE_CONCORDE
//#define USE_LINKERN

// Set the variable to the path to concorde directory
#define CONCORDE_PATH string("./concorde")